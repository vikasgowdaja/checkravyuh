# Chess Engine Package

This package holds chess-specific primitives and interfaces.

Responsibilities:

- notation types such as FEN and PGN
- move representations
- position validation interfaces
- board-state abstractions that UI and data layers can consume

It should remain free of Flutter UI code.