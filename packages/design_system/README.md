# Design System

Shared Flutter widgets, tokens, typography, spacing rules, and board-adjacent presentation components live here.

The package exists so the mobile app and future Flutter surfaces can share consistent design primitives without duplicating UI code.