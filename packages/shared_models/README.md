# Shared Models

Pure Dart entities shared across mobile features and supporting libraries.

Constraints:

- no Flutter imports
- no HTTP concerns
- stable semantics for traps, scenarios, progress, and review state