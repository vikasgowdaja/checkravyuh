# GitHub Conventions

This folder will hold CI workflows, pull request templates, and repository automation once the runnable Flutter and NestJS apps are generated.

Initial expectations:

- pull requests run targeted checks for the surfaces they change
- mobile, API, and admin pipelines evolve independently
- content changes validate authored trap files before merge